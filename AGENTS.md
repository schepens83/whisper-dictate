# Agent Instructions

See `CLAUDE.md` for full project context, architecture, and conventions.

Read the entire codebase before making any changes. Pay particular attention to the ordering of operations in `main.go` — the PID file and waybar signal timing are load-bearing.

Always deploy via `./deploy.sh`, never manually.
